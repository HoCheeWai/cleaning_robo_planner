import styles from './Footer.module.css';

export function Footer() {
  return (
    <footer className={styles.footer}>
      Cleaning Robot Fleet Calculator · All calculations run client-side
    </footer>
  );
}
